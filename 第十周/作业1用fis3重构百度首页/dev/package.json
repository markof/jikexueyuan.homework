{
  "name": "dev",
  "version": "1.0.0",
  "description": "为了能够正确的获取天气，这里需要通过浏览器获取用户的地理信息。如果不能获取则使用默认的“北京”。 目前在PC端获取地理信息有两种方法：   1.通过IP在服务器端判断IP所在的地理信息。   2.通过浏览器提供的HTML5的地理信息API。",
  "main": "fis-conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fis-optimizer-html-minifier": "0.0.9",
    "fis3-hook-cmd": "^0.1.2",
    "fis3-postpackager-loader": "^1.3.12"
  }
}
